# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
expo-env.d.ts

# native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
android/
ios/

# metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macos
.DS_Store
*.pem

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
.gstack/

# detox
/artifacts/
ios/build/


/.idea/
/.vscode/

# openapi-generator output — regenerated from api.yaml via `pnpm generate:mock`
# / `pnpm generate:api` (see package.json). The E2E fixture logic that would
# otherwise be lost on regeneration lives in e2e/mock-server-overrides/
# (committed) and is reapplied automatically as part of generate:mock — see
# e2e/apply-mock-overrides.js.
src/api/
mock-server/

# winston logs written by the mock server when it runs
combined.log
error.log
