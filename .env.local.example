# Your Sofia Mobile App - Environment Configuration
# Copy this file to .env.local and update the values

# Development API (local Payload CMS)
EXPO_PUBLIC_API_URL=http://localhost:3000

# Staging API (optional - for testing against staging environment)
EXPO_PUBLIC_STAGING_API_URL=http://local-IP:3003

# Production API
EXPO_PUBLIC_PRODUCTION_API_URL=https://your.sofia.bg

# Note: The app will use the environment switcher to select which API to connect to
# In development builds (__DEV__), you can switch between environments at runtime
# In production builds, only the production API will be used
